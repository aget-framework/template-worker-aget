# Subagent Definitions

Place subagent definition files here. Each `.md` file defines a specialized
subagent that can be invoked via the Task tool or --agent flag.

See: https://docs.anthropic.com/en/docs/claude-code/sub-agents
