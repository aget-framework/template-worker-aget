# Context Rules

Place path-scoped context rules here. Rule files are automatically loaded
when working in matching directories.

Example: `governance.md` could contain rules that apply when editing
governance/ files.

See: Claude Code documentation on rules files.
